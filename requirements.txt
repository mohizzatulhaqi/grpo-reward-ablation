# Core training/eval (GPU box)
torch>=2.3
transformers>=4.45
trl>=0.12
datasets>=2.20
accelerate>=0.34

# QLoRA / free-tier mode (4-bit base + LoRA adapter)
peft>=0.11
bitsandbytes>=0.43

# Analysis + tests (runs anywhere, no GPU)
scipy>=1.11
numpy>=1.26
pytest>=8.0

# Optional: vLLM-accelerated generation for training (--use-vllm) and eval.
# Heavy dep with strict CUDA needs; install separately on the GPU box if wanted:
#   pip install vllm
# The code auto-falls back to standard generation if it's missing or fails.
# vllm>=0.6
